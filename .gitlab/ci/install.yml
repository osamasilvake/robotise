client:
  stage: Install
  image: node:15-alpine
  script:
    - yarn install
  artifacts:
    name: "root"
    paths:
      - node_modules
    expire_in: 15 minutes
  only:
    - merge_requests
    - master
    - production
