client:
  stage: Install
  before_script:
    - cd client/
  script:
    - yarn install
  artifacts:
    name: "client"
    paths:
      - node_modules
    expire_in: 15 minutes
  only:
    - merge_requests
    - master
    - production

e2e:
  stage: Install
  before_script:
    - cd e2e/
  script:
    - yarn install
  artifacts:
    name: "e2e"
    paths:
      - node_modules
    expire_in: 15 minutes
  only:
    - merge_requests
    - master
    - production

root:
  stage: Install
  script:
    - yarn install
  artifacts:
    name: "root"
    paths:
      - node_modules
    expire_in: 15 minutes
  only:
    - merge_requests
    - master
    - production
