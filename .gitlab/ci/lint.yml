eslint:
  stage: Lint
  dependencies:
    - root
  script:
    - yarn lint:es
  only:
    - merge_requests
    - master
    - production

stylelint:
  stage: Lint
  dependencies:
    - root
  script:
    - yarn lint:scss
  only:
    - merge_requests
    - master
    - production
