npm-install:
  stage: Install
  image: node:13-alpine
  script:
    - npm install
  artifacts:
    paths:
      - node_modules
    expire_in: 30 minutes
  only:
    - merge_requests
    - master
    - production
