prettier:
  stage: Quality
  image: node:15-alpine
  script:
    - yarn pretty:check
  only:
    - merge_requests
    - master
    - production

lint:
  stage: Quality
  image: node:15-alpine
  script:
    - yarn lint:es
  only:
    - merge_requests
    - master
    - production

test:
  stage: Quality
  image: node:15-alpine
  script:
    - yarn test:ci
  only:
    - merge_requests
    - master
    - production
