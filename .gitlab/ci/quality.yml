prettier:
  stage: Quality
  image: node:15-alpine
  script:
    - yarn pretty:check
  only:
    - merge_requests
    - master
    - production

eslint:
  stage: Quality
  image: node:15-alpine
  script:
    - yarn lint:es
  only:
    - merge_requests
    - master
    - production
