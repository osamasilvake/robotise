Q1-prettier:
  stage: Quality
  image: node:15-alpine
  script:
    - yarn pretty:check
  only:
    - merge_requests
    - development
    - production

Q2-lint:eslint:
  stage: Quality
  image: node:15-alpine
  script:
    - yarn lint:es
  only:
    - merge_requests
    - development
    - production

Q3-test:jest:
  stage: Quality
  image: node:15-alpine
  script:
    - yarn test:ci
  only:
    - merge_requests
    - development
    - production
