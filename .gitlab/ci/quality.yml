prettier:
  stage: Quality
  dependencies:
    - root
  script:
    - yarn pretty:check
  only:
    - merge_requests
    - master
    - production

eslint:
  stage: Quality
  dependencies:
    - root
  script:
    - yarn lint:es
  only:
    - merge_requests
    - master
    - production
