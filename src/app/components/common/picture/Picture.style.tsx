import { makeStyles } from '@material-ui/core/styles';

export const PictureStyle = makeStyles(() => ({
	sImage: {
		display: 'block',
		margin: '0 auto'
	},
	sImageFullWidth: {
		width: '100%'
	}
}));
