import { Theme } from '@mui/material/styles';
import { makeStyles } from '@mui/styles';

export const GeneralAllOrderTableStyle = makeStyles((theme: Theme) => ({
	sCaption: {
		fontStyle: 'italic',
		marginLeft: theme.spacing(0.5)
	}
}));
