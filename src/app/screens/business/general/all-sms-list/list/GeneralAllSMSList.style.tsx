import { Theme } from '@mui/material/styles';
import { makeStyles } from '@mui/styles';

export const GeneralAllSMSListStyle = makeStyles((theme: Theme) => ({
	sBox: {
		marginTop: theme.spacing(3)
	}
}));
