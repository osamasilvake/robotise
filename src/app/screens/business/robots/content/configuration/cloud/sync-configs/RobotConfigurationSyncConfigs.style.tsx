import { Theme } from '@mui/material/styles';
import { makeStyles } from '@mui/styles';

export const RobotConfigurationSyncConfigsStyle = makeStyles((theme: Theme) => ({
	sButton: {
		marginTop: theme.spacing(2)
	},
	sButtonGap: {
		marginLeft: theme.spacing(1)
	}
}));
