import { Theme } from '@mui/material/styles';
import { makeStyles } from '@mui/styles';

export const RobotInventoryListStyle = makeStyles((theme: Theme) => ({
	sBox: {
		marginTop: theme.spacing(2)
	}
}));
