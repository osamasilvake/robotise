import { Theme } from '@material-ui/core/styles';
import { makeStyles } from '@material-ui/styles';

export const RobotsInventoryHeadStyle = makeStyles((theme: Theme) => ({
	sTitleBox: {
		marginBottom: theme.spacing(1.5)
	}
}));
