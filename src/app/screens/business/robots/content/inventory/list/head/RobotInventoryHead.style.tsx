import { Theme } from '@mui/material/styles';
import { makeStyles } from '@mui/styles';

export const RobotInventoryHeadStyle = makeStyles((theme: Theme) => ({
	sTitleBox: {
		marginBottom: theme.spacing(1.5)
	}
}));
