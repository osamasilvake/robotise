import { Theme } from '@mui/material/styles';
import { makeStyles } from '@mui/styles';

export const RobotPurchaseCommentStyle = makeStyles((theme: Theme) => ({
	sBox: {
		marginTop: theme.spacing(1.5)
	}
}));
