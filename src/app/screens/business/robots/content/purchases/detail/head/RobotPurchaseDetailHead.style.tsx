import { makeStyles, Theme } from '@material-ui/core/styles';

export const RobotPurchaseDetailHeadStyles = makeStyles((theme: Theme) => ({
	sHeadBox: {
		margin: theme.spacing(2.5, 0, 1)
	},
	sHeadBilled: {
		marginBottom: theme.spacing(0.4)
	}
}));
