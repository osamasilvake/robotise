import { Theme } from '@mui/material/styles';
import { makeStyles } from '@mui/styles';

export const SiteConfigurationPaymentSettingsStyle = makeStyles((theme: Theme) => ({
	sExcerpt: {
		marginBottom: theme.spacing(1.5)
	},
	sSubmit: {
		marginTop: theme.spacing(1)
	}
}));
