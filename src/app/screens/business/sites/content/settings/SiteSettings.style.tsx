import { Theme } from '@mui/material/styles';
import { makeStyles } from '@mui/styles';

export const SiteSettingsStyle = makeStyles((theme: Theme) => ({
	sBox: {
		marginTop: theme.spacing(3)
	}
}));
